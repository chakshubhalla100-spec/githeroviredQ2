Created new repository githeroviredQ2

with public repository --- https works for 1 kb file 

testing 200 mb file now 

Created new repository githeroviredQ2 github
clone folder into local github folder git clone url
created a new branch git checkout -b lfs

Ran command git lfs install
To track lfs  ran command git lfs track "*.zip"

added 200 mb txt file to repository folder and ran the git add . /git status / git commit -m 'added 200 mb file' / git status 

git lfs install
git lfs track "*.txt"
cat .gitattributes
git add .gitattributes
git add dummy200mb.txt
git commit -m "Add  file using Git LFS"
git status
git push origin main

note :  branch should match while pushing

Followed again with big size file 200 mb and give a successful push to the github.

For private repository :  Ssh works 

